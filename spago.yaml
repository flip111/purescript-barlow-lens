package:
  name: barlow-lens
  dependencies:
  - either
  - foldable-traversable
  - lists
  - maybe
  - newtype
  - prelude
  - profunctor
  - profunctor-lenses
  - tuples
  - typelevel-prelude
  test:
    main: Test.Main
    dependencies:
      - spec
      - spec-discovery
workspace:
  package_set:
    registry: 50.5.0
  extra_packages:
    spec-discovery:
      dependencies:
        - aff
        - aff-promise
        - effect
        - foldable-traversable
        - prelude
        - spec
      git: https://github.com/purescript-spec/purescript-spec-discovery.git
      ref: master

